# 作業状況（2026-09-06 時点）

**完成して稼働中。** 以降は不具合の修正と改善が中心。

- 公開先: **https://aoe99.github.io/sc-am2-study/app/**（アプリ名: SC試験対策）
- 問題データ:
  - 午前 19回 × （午前I 30問 + 午前II 25問）= **1,045問**
  - 午後 19回 = **90事例 / 838設問**（記号選択113 / 語句308 / 記述417）、図表777枚
- 検証はすべて通過（`tools/08_validate.py` の全項目が ✓）

---

## 修正を依頼されたときの手順

**別コンテキストで指示を受けた場合も、修正から公開・コミットまで通しで行う。**
途中で止めて確認を取る必要はない（初期開発時のフェーズ確認はもう不要）。

### 1. 原因を実データで特定する

推測で直さない。必ず現物を見る。

```bash
# 対象の問題が今どうなっているか
python3 -c "import json;d=json.load(open('data/questions.json'));q=[x for x in d['questions'] if x['id']=='R05haru-am2-17'][0];print(q)"

# OCRの生の座標（不具合の原因はたいていここに出る）
python3 - <<'PY'
import importlib.util, sys
sys.argv=['x','--section','am2']          # am1 / am2
spec=importlib.util.spec_from_file_location('p','tools/04_parse.py')
p=importlib.util.module_from_spec(spec); spec.loader.exec_module(p)
lines=p.load('R05haru'); h=p.find_headings(lines); order=sorted(h)
k=order.index(17); end=h[order[k+1]] if k+1<len(order) else len(lines)
for l in lines[h[17]:end]:
    print(f"x={l['x']:.3f} y={l['y']:.3f} h={l['h']:.3f} | {l['text'][:50]!r}")
PY
```

画像の不具合は **切り出した画像を実際に開いて見る**。OCRで中身を読ませて照合すると
確実（`sips -Z 700 data/figures/... --out /tmp/x.png` して Read、または
`./bin/pdfkit-tool ocr /tmp/x.png`）。

### 2. 直す

データ側は `tools/` を、アプリ側は `app/` を編集する。手で直せる設定は
`tools/corrections.json`（OCRの既知の誤読）と `tools/tags.json`（分野タグ）。

### 3. 作り直す

```bash
# 午前
python3 tools/04_parse.py   --section am1   # 抽出を変えたとき
python3 tools/04_parse.py   --section am2
python3 tools/05_figures.py --section am1   # 図表の切り出しを変えたとき
python3 tools/05_figures.py --section am2

# 午後（--section は取らない。区分がひとつしかない）
python3 tools/11_pm_answers.py              # 解答例 → 設問の骨格
python3 tools/12_pm_commentary.py           # 採点講評
python3 tools/13_pm_explanations.py         # 教科書解説
python3 tools/14_pm_parse.py                # OCR → 事例本文・設問文
python3 tools/15_pm_figures.py              # 図表の切り出し

python3 tools/06_build.py                   # 統合（常に必要）
python3 tools/09_duplicates.py --apply      # 午前だけが対象
python3 tools/10_pack.py                    # → sc-data-{am,pm}.json + -figures.bin
python3 tools/08_validate.py                # 全項目が ✓ になること
```

**午後は「解答から問題文へ」の順に依存している。** `11_pm_answers` が設問の骨格
（どの設問に、どの空欄があるか）を作り、`14_pm_parse` はその記号をOCRした問題文から
探す。11を流し直したら14も流し直すこと。

**各ステージの終了コードを必ず確認する。** 出力を `head` などで切ると SIGPIPE で
ファイル書き込み前に落ちるのに気づけない（実際に一度やった）。

### 4. アプリを変えたら Service Worker の版を上げる

```bash
# app/sw.js の VERSION を v13 → v14 のように上げる
```

**これを忘れると端末に修正が届かない。** キャッシュされた古いモジュールが動き続ける。

### 5. コミットして公開

```bash
git add -A && git commit -m "…" && git push origin main
```

コミットメッセージは日本語で、**何を直したかではなく、なぜそうなっていたか**を書く。
末尾に `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>` を付ける。

GitHub Pages への反映は1〜2分。確認:

```bash
curl -s https://aoe99.github.io/sc-am2-study/app/sw.js | grep -o "sc-am2-v[0-9]*"
```

### 6. 利用者に伝えること

- **データを作り直したなら**「新しいパックの読み込みが必要」と明記する。どの区分を
  作り直したかで渡すファイルが変わる:
  - 午前 → `data/sc-data-am.json` ＋ `data/sc-data-am-figures.bin`
  - 午後 → `data/sc-data-pm.json` ＋ `data/sc-data-pm-figures.bin`

  **区分ごとに独立している。** 午後だけ入れ直しても午前の1,045問と学習記録は残る。
  ホーム画面下部に出る「問題データ 2026-09-04 06:18」は**選んでいる区分のもの**
- アプリだけの変更なら読み込み直しは不要

---

## やってはいけないこと

- **`data/` を公開しない。** IPA の問題冊子と翔泳社の解説はどちらも著作物。
  `.gitignore` で除外済みだが、コミット前に `git diff --cached --name-only` で確認する
- IndexedDB名 `sc-am2`、localStorageキー `sc-am2:settings`、書き出しの
  `kind: sc-am2-progress` を変えない。**識別子であって表示名ではない。**
  変えると読み込み済みの1,045問と学習記録が孤立する
- **走行状態（kv の `run`）は、解答画面が保存と削除の両方をすること。** 保存だけして
  削除しないと、採点し終えた走行が残り続けてホームに「中断した学習 0問」が出続ける
- **問題IDを変えない。** `states` は questionId をキーにしている。午後の
  `R07aki-pm-1-2-3`（回-区分-大問-設問-小問）も同じで、採番を変えると自己採点の
  履歴が全部孤立する
- **図表を base64 でJSONに戻さない。** 午後は777枚・73MBあり、data: URI にすると
  パックが84MBになって iPhone の Safari が落ちる。`.bin` は ArrayBuffer から
  Blob を切り出すだけで、文字列にもJSONにもしない
- サブセット実行（`--section am2 R05haru` のように回を指定）しても中間ファイルは
  マージ書き込みになっている。全回を消さないこと

---

## 残っている品質課題

### 午後

**設問文は 838/838 揃っている。**

2026-09-05 まで 778/839 だった。「上余白ごと切れたスキャン」だと思っていたものは
**こちらの描画の誤り**で、`/Rotate 90` のページを回転前の枠の大きさで描いていたため
紙の上から3割が画布の外に落ちていた（`tools/swift/pdfkit-tool.swift` の `displayBox`）。

**解説があるのは 824/838。** 残り14件は教科書解説側にその設問の見出しが立っていない回。

**記号の読めない空欄と、ただの隙間は区別できない。** 2026-09-04 に手がかりを4つ測った
（`data/build/pm/pages/` の画像とOCRの座標で再現できる）。

| 手がかり | 結果 |
|---|---|
| 隙間の幅 | 分離しない。本物の中央値0.049 / それ以外0.078で重なりが大きい |
| 枠線の形（上下の横罫線を検出） | 記号入りの枠758個のうち検出できたのは81%。フィルタにすると本物の19%を失う |
| 設問から数えた「本文にあるべき数」 | 594に対し実際885。事例ごとのばらつきが大きく削る根拠にならない |
| インク量（ほぼ真っ白か） | **安全だが取り分が少ない。** しきい値0.02で本物の損失1.6%・ノイズ除去11%。本文の散文だけなら約60個 |
| 枠だけを600dpiで読み直す（2026-09-06） | **効いた。** 記号入りが589→983個。採用時の正確さ97% |

インク量による除去は 2026-09-04 に実装した（`bin/pdfkit-tool ink` ＋
`tools/14_pm_parse.py` の `INK_MIN`）。枠の総数はほぼ変わらないが、残っている枠は
すべて「紙に何かが刷られている」ものになった。

**本文の空欄の枠は、記号が読めているものが983個、読めていないものが886個。** 素直に
隙間を拾うと6,865個になり、表の列の罫線と図の配置が混ざる。縦に揃う隙間（同一ページで
3行以上）、1行に2つ以上ある枠、紙が白いままの隙間を落としている。`tools/14_pm_parse.py`
の `column_edges`・`drop_layout_boxes`・`INK_MIN`。

**枠だけを600dpiで切り出して読み直すと、記号は589個から983個になった。**
（`pdfkit-tool rectocr` ＋ `resolve_frames`）矩形の中で見つけた文字の並びを1かたまり
ずつ返し、かたまりごとに、その大問の解答例が挙げるラベルに一致する候補が**ちょうど
1つ**のときだけ採用する。隙間ひとつに枠が2つ入ることがあるため（「［f］は［g］して
おく」）。ラベルにならなかったかたまりは捨てる。ページ読みで記号が取れていた849個を
正解にして較正したところ、**採用時の正確さ97%・復元率78%**。枠の上下の取り方を変えて
3回読み、同じ位置のかたまりの候補を合わせている。

**設問文の空欄は解答例と突き合わせて直している。** 枠の右罫線が二度写る（84件）、
記号を挟んで枠が二つに割れる、範囲の左端が閉じ罫線だけになる、といった崩れ方を
`tools/06_build.py` の `pm_wording` で順に直す。設問文の枠がラベルと完全に一致するのは
295/428。残りは範囲表記（「［a］～［e］」の2枠に対しラベルは5つ）が大半。

**記号のある枠のうち133個は空欄ではなかった。** 本文が本当に角括弧を刷っている場合
（`argv［1］`、検索語の `［チョコ］`、行から切り出された助詞 `［を］`）。事例に
`blanks` を持たせ、アプリはそこにある記号だけを空欄として強調する。強調される枠は
809個で、うち461個が本文の行に見えている。残り348個は図の中にあり、絵が引き継ぐ。

**長いキャプションを拾えていなかった（2026-09-08 に直した）。** 「キャプションは中央
寄せ」の判定が長いものでは崩れ、12件が散文扱いで切り出されずにいた。散文は図番号の
直後に助詞が来る（「図1の設定では」）がキャプションは名詞に入る（「図1 クラウド
サービス利用に関する…」）という違いで分けた。図表 762→777枚、図表リンク 471/471。
絵に入らなかった断片の字下げ表示（`figtext`）も、段落の途中に字下げが出るのは
組版の誤りにしか見えないのでやめた（1,428行→0）。

**図の中身かどうかは、行の書かれ方で決める（2026-09-07 に直した）。** 最初は左端の
位置で判定していて、余白を3つ持つ事例（コード0.115／段落0.15／会話文の折り返し0.23）で
基準を取り違え、本文の48%を絵に置き換えていた。平30春 午後I 問1 は155行中140行、
T主任とUさんの会話が丸ごと消えていた。いまは「語中に空白が無く、文と言える長さの
日本語がある行」を散文とする（`14_pm_parse.py` の `looks_prose`）。絵になる行は
6,887→6,660（本文の47%）。図表は777枚。**表は矩形の中を全部隠す** — キャプションが表の
上にあり走査は表の下の散文で止まるので矩形の中は全部セルであり、セルを読み順に並べても
読めない。図はキャプションが下にあり矩形が導入文まで伸びるので、書かれ方で決めるまま。隠す行はキャプションの向きに歩いて決め、**絵の矩形の
中にあること**を条件にする（切り出しに入っていない行を隠すと絵にも本文にも無くなる。
141行がそうなっていた → 0）。表のセルを読み順に潰した行は散文と見分けが付かないので、
散文に見える行を1行だけ飛び越し、2行続いたら終わりにする（`06_build.py` の
`pm_drawing_rows`、アプリは行の `drawn` 印を見る）。

**ページ番号は落としている。** 紙の下端の `- 12 -` が独立した行として本文に入り、
ページをまたぐ設問は末尾に連れてくる。罫線状の記号が数字に接していることを条件に
落とす（裸の数字は字数や項番、「ケ SHA-512」はハイフン＋数字）。設問文110件・本文257行。

**読めない記号は、最後は本文の並びから置いている。** 設問が問うているのに本文に一度も
出てこないラベルは、前後のラベルに挟まれた空の枠に入れる。枠すら無いときは、素の記号が
助詞のあいだに1回だけ立っていればそれを枠にし、**行ごと落ちているときは行間の広さ**
（そのページの行送りの中央値の2.5倍以上あく場所がちょうど1つ）で行を作って
［j］ を置く。令7秋 問1 の ［j］ がこれ（`tools/06_build.py` の
`pm_place_missing_blank`）。

**読点は「，」に戻している。** IPAは全角カンマで組むが Vision は「、」と読む。同じ
冊子の解答例・採点講評はテキスト層で「，」1,604回・「、」0回、tesseract も独立に
カンマと読む。午前は前からこうしていて、午後だけ抜けていた（`14_pm_parse.py` の
`PUNCT`、14,110箇所）。教科書解説は翔泳社が本当に両方使うので触らない。

**カタカナ語は事例内の多数決で直している。** 事例に1度だけ現れて、同じ事例の多い語と
1文字違いなら誤読（`サーノ`→`サーバ`、19語）。実在語を巻き込まないよう、テキスト層
60万字（教科書解説＋解答例＋採点講評）に載っている綴りは対象外にし、候補が2つある語は
保留する（`ホール` は `メール` とも `ツール` とも1文字違い）。`06_build.py` の
`pm_vote_terms`。

**OCRエンジンの入れ替えは測って見送った（2026-09-07）。** macOS 26 の
`RecognizeDocumentsRequest` を試作して24ページ・漢字5,100字で比べたところ、解説に無い
漢字の率が現行 0.58% に対し 1.03% と倍悪い。表の構造は10ページ中7ページで取れるが、
本題の「表の中の空欄記号」は5件試して回収1件（現行も1件）。画像の分割・高dpi・
`pdfimages` での取り出しも効果ゼロ（元スキャンは33冊すべて実質200dpi、現行の400dpi
描画はその拡大）。言語補正は入れると悪化する（ページ全体で2.82%→5.24%、「Fアプリ」を
「「アプリ」にする）。

**規則で届かない空欄25個は、ページを見て置いた（2026-09-08）。** `tools/pm_blanks.json`
に事例ごとの文字列置換として26件。枠ごと落ちていたもの、行頭行末で切れていたもの、記号が
枠の外に出ていたもの、枠の中の記号を別の文字と読んでいたもの（`1`→`i`、`b`→`q`、
`a`→`α`）。`find` が事例に1回現れることを検査するので、抽出が変わって当たらなくなれば
`review.md` に出る。届かない参照のある設問は 31→8 になり、残る8件のうち6件は設問文側の
余分な角括弧（解答群や読み残しの ［ ］）でリンクにしないのが正しく、実質は下線の丸数字2個。

**下線の丸数字は、設問が参照する388個のうち386個（99.5%）が本文にある。** 残り2個は
OCRが落としている。アプリは押されたときに「読み取れていません」と出す。下線そのもの
（線）は図形なので、どの語句までが下線かは復元できない。丸数字の位置だけを示している。

読めなかった丸数字は**順序から復元している**。丸数字は本文を上から①②③…と進み、設問は
その全部を名指しする。だから設問が参照しているのに本文にない番号は、「その前後の番号の
あいだにあって、そこでは順序が崩れている」丸数字がただ一つなら、それに違いない。令5秋
問3の⑦は⑥の3行下・⑧の2行上に**②として**写っていた（`tools/06_build.py` の
`pm_reorder_markers`、8件）。

**図表はキャプションのある777件を切り出せている。** 図は
「取りこぼすより広めに取る」方針なので、上に導入文が1行入ることがある。

**図の中の文字は本文に出さない。** 切り出した絵とその絵から読んだ断片を並べると、
`a を送` `→` `（5） 9` のような読めない行が絵の横に並ぶ。キャプションから外向きに
歩いて、組み方が本文でない行（余白から始まって measure まで届く／句点で終わる／
折り返した幅広の段落、のいずれでもない行）を図の中身とし、アプリは絵に置き換える。
1,674行が絵になる。隠した行の空欄記号と
丸数字は絵が引き継ぐので、設問からのリンクは絵に飛ぶ（`tools/14_pm_parse.py` の
`mark_drawings`）。図の高さの上限 `MAX_FIG_H` は 0.62 では足りず、令7春 問3 の 図9 が
レーン見出しごと切り落とされていたので 0.70 にした。

`data/build/pm/answers.json` の `flags` に「表の列を推定」が19件。IPAの解答例PDFは
2列の解答（`利点`／`内容` など）をテキスト層で1行に潰してしまい、折り返しと区別が
つかない。分けたほうが原文に近いので分けているが、目視の価値がある。

### 午前

`data/build/review.md` に要確認179問。大半は「選択肢が表形式（画像化済み）」と
「解説に存在しない漢字」（誤検出を半分程度含むヒント）。

`data/build/cross-check.md` に**回をまたいだ不一致**がある。同じ問題が複数の回に
出ているため、抽出が食い違う箇所はどちらかが誤読。ここを潰すのが最も効率がよい。
ただし `探索`↔`発見`、`登録`↔`記載` のように**IPAが実際に文言を変えている**ものも
混ざるので、機械的に統一してはいけない。

分野タグは23種。「その他」が114問残っており、`tools/tags.json` にキーワードを
足せば減る。

## 未確認

- iPhone 実機での「ホーム画面に追加」。表示は 375px エミュレーションで確認済み
- Safari(mac) の実機表示

## 今後の拡張

- `meta.sections` に問数と制限時間があるので、区分を増やしても本番モードは追従する
- 解答履歴の `mode` は自由文字列なので、新モードを足しても既存記録と衝突しない
- 午後の記述は自己採点なので、`answers` に書いた本文（`typed`）が溜まる。同じ設問を
  解き直したときに前回の答案を並べて見せる余地がある
- `commentaryRate`（正答率 高/やや高/平均/やや低/低）は保存してあるが、まだ絞り込みに
  使っていない。「正答率が低い設問だけ」の練習は足せる
