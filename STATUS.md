# 作業状況（2026-09-03 時点）

## 完了

### Phase 1 — 問題データの作成 ✅
19回 × 25問 = **475問**。`data/questions.json`。検証項目はすべて通過。

| 項目 | 結果 |
|---|---|
| 各回25問 / 合計475問 | ✓ |
| 全問に 問題文・選択肢4つ・正解・解説 | ✓ |
| IPA解答例と教科書解説の正解の一致 | ✓ 475/475 |
| 制御文字・文字化け・連続空白・ID重複 | ✓ なし |
| 図表付き | 74問 |
| 選択肢自体が表・図 | 38問（うち33問に画像を生成） |
| 要確認 | 88問（`data/build/review.md`） |

### Phase 2 — 重複問題の統合 ✅
- **再出題 56グループ / 実質 400問**（最多5回）
- 「同じ設問文で選択肢が異なる」33組は統合していない（正解が変わるため）
- `data/build/duplicates.md` / `cross-check.md`

### Phase 3 — Webアプリ ✅
`app/`。ビルド工程なし、HTML + CSS + ES modules のみ。外部依存ゼロ。

- 4モード（練習 / 本番 / 復習 / 年度別）すべて動作確認済み
- 単一ファイル `data/sc-data.json`（約10MB、図表206枚を data: URI で内蔵）を
  ファイル選択で読み込み、IndexedDB に保存
- 学習記録の書き出し／読み込みは往復検証済み（55解答・53状態が完全一致）
- ライトナー5箱の間隔（当日/1日/3日/7日/16日、箱5正解で35日）を検証済み
- 選択肢シャッフルは既定ON。解説を開くと元の並びに戻り、位置が変わった選択肢には
  「解答時は ウ」と併記
- iPhone(375px) でのレイアウトを確認済み

## 未確認

- **Service Worker（オフライン動作・ホーム画面追加）を実機で確認していない。**
  検証に使った埋め込みブラウザが Service Worker の登録を拒否するため
  （`sw.js` は 200・MIME も secure context も正常、"unknown error" で失敗）。
  シェル21ファイルはすべて取得できることを確認済みなので、コード側の問題では
  ないと考えているが、**実機の Safari / Chrome での確認が必要**。
- Chrome / Safari(mac) / Safari(iOS) の実機表示。検証は埋め込み Chromium のみ。

## 残っている品質課題

`data/build/review.md` の88問。内訳:

| 件数 | 指摘 | 対応 |
|---|---|---|
| 約38 | 選択肢が表形式（画像化） | 33問は画像生成済み。残5問は下記 |
| 約35 | 解説に存在しない漢字 | ヒント。半分程度は誤検出（模倣・楕円など） |
| 11 | 空の選択肢 | すべて画像化対象 |
| 10 | 選択肢マーカーを位置から補完 | 目視確認したい |

**選択肢の画像が作れなかった5問**（マーカー位置の推定が不確かなため）:
`R01aki 問17` / `R01aki 問22` / `R03haru 問12` / `R05aki 問17` / `R06aki 問7`

**既知だが未修正**:
- `R07aki-am2-16` 選択肢エ — `“<”，“>”`／`“&lt;”，“&gt;”` が壊れている（合意により手動対応）
- `RefLection` — 英単語復元の誤爆（`tools/corrections.json` で潰せる）

`data/build/cross-check.md` に**回またぎの不一致**がある。同じ問題が複数の回に
出ているため、抽出が食い違う箇所はどちらかが誤読。ここを潰すのが最も効率がよい。
ただし `探索`↔`発見`、`登録`↔`記載` のように**IPAが実際に文言を変えている**ものも
混ざるので、機械的に統一してはいけない。

## 今後やるなら

開発プロンプト §8 の拡張余地は残してある。
- `id` は `R07aki-am2-01` の形式で区分（`am2`）を含むので、午前I は `am1`、
  午後は `pm` を足せばよい
- `sessions[].examName` と `schemaVersion` があるので、スキーマ変更時に移行できる
- 解答履歴の `mode` は自由文字列なので、新モードを足しても既存記録と衝突しない

## 使い方

```bash
python3 tools/serve.py        # → http://localhost:8765/
```

初回に `data/sc-data.json` を「ファイルを選ぶ」から読み込む。

データを作り直すときは:

```bash
./tools/run_all.sh            # 全19回（OCRはキャッシュされる）
python3 tools/09_duplicates.py --apply
python3 tools/10_pack.py      # → data/sc-data.json
```
